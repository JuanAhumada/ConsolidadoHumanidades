# Consolidado de Humanidades
# Instalación limpia (PowerShell, con el venv activo):
#   pip freeze > installed.txt
#   pip uninstall -y -r installed.txt
#   pip install -r requirements.txt

# Núcleo (consolidado Excel + CLI)
polars>=1.0.0
fastexcel>=0.10.0
openpyxl>=3.1.0

# GUI de escritorio (python main.py --gui)
customtkinter>=5.2.0
pillow>=10.0.0

# Interfaz web HTML (python main.py)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Empaquetado .exe (build_exe.bat) — opcional en desarrollo
pyinstaller>=6.0.0
