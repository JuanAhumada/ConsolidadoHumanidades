# Consolidado de Humanidades — dependencias unificadas
# Instalación: pip install -r requirements.txt

# Ejecución (app + CLI)
polars>=1.0.0
fastexcel>=0.10.0
openpyxl>=3.1.0
customtkinter>=5.2.0
pillow>=10.0.0

# Empaquetado ejecutable (build_exe.bat)
pyinstaller>=6.0
